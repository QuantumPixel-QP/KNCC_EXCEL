{
  "framework": "vite",
  "buildCommand": "cd platform/frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "platform/frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" }
  ]
}
