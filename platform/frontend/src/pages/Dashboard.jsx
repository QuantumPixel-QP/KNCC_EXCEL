import { apiFetch } from '../utils/api';
"/**\n * Dashboard — shows live KPIs from /api/materials/{id}/summary per project,\n * plus per-project cards with cost, delivery %, material count.\n */\nimport React, { useState, useEffect } from 'react';\nimport { useNavigate } from 'react-router-dom';\nimport { useProject } from '../context/ProjectContext';\nimport { AreaChart, Area, BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Cell } from 'recharts';\n\nconst API = 'http://localhost:8000/api';\n\nconst fmt$ = v => v != null ? '$' + Number(v).toLocaleString('en-US', { maximumFractionDigits: 0 }) : '$0';\n\nconst MiniProgressBar = ({ pct, color = 'var(--accent-blue)' }) => (\n  <div style={{ height: '4px', background: 'rgba(255,255,255,0.08)', borderRadius: '2px', marginTop: '8px' }}>\n    <div style={{ height: '100%', width: `${Math.min(100, pct || 0)}%`, borderRadius: '2px', background: color, transition: 'width 0.8s ease' }} />\n  </div>\n);\n\nconst KpiCard = ({ label, value, sub, color, icon }) => (\n  <div className=\"glass-panel\" style={{ padding: '20px 24px', borderTop: `3px solid ${color}` }}>\n    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: '10px' }}>\n      <div style={{ fontSize: '11px', color: 'var(--text-secondary)', textTransform: 'uppercase', letterSpacing: '1px' }}>{label}</div>\n      <span style={{ fontSize: '20px' }}>{icon}</span>\n    </div>\n    <div style={{ fontSize: '28px', fontWeight: 700, color }}>{value}</div>\n    {sub && <div style={{ fontSize: '12px', color: 'var(--text-secondary)', marginTop: '4px' }}>{sub}</div>}\n  </div>\n);\n\nconst Dashboard = () => {\n  const { projects, activeProject, setActiveProject, loading: projLoading } = useProject();\n  const navigate = useNavigate();\n  const [summaries, setSummaries] = useState({});\n  const [loadingSummaries, setLoadingSummaries] = useState(false);\n\n  useEffect(() => {\n    if (!projects.length) return;\n    setLoadingSummaries(true);\n    Promise.all(\n      projects.map(p =>\n        
<truncated 9765 bytes>